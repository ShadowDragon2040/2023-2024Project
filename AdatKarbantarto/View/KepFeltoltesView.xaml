<UserControl x:Class="AdatKarbantarto.View.KepFeltoltesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdatKarbantarto.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <WrapPanel HorizontalAlignment="Center" DockPanel.Dock="Top">
            <TextBox Name="txtEditor" Text="{Binding EditorText}" Background="White" Width="150" Height="30" />
        </WrapPanel>
            <Button x:Name="btnOpenFile" Content="Open file" Command="{Binding uploadpressed}"/>
            <Button Content="upload" Command="{Binding buttonpressed}" Margin="0,246,0,172"/>
    </Grid>
</UserControl>
